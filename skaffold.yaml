apiVersion: skaffold/v2beta26
kind: Config
metadata:
  name: skaffold-telepresence-demo
build:
  local:
    useBuildkit: true
    useDockerCLI: true
  artifacts:
  - image: demo-app
    context: .
    docker:
      dockerfile: docker/Dockerfile
deploy:
  kustomize:
    paths:
    - kustomize/base
